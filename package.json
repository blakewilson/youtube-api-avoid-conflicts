{
  "name": "youtube-api-avoid-conflicts",
  "version": "1.0.0",
  "description": "A WordPress plugin to avoid conflits with the YouTube API onYouTubePlayerAPIReady() function",
  "main": "youtube-api-avoid-conflicts.php",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Push Labs",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-uglify": "^3.0.0"
  }
}
